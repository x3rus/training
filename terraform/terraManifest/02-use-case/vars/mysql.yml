mysql_root_password: super_ultime_pass
mysql_databases:
  - name: contact
    encoding: latin1
    collation: latin1_general_ci
  - name: showpi
    encoding: latin1
    collation: latin1_general_ci
mysql_users:
  - name: "{{ my_cont_user }}"
    host: "%"
    password: "{{ my_cont_pass }}"
    priv: "contact.*:ALL"
  - name: "{{ my_pi_user }}"
    host: "%"
    password: "{{ my_pi_pass }}"
    priv: "showpi.*:ALL"
